import React from "react";
import styled from "styled-components";

/* const Line = styled.div`
  padding: 0
    ${({ horizontalPadding }) => horizontalPadding && horizontalPadding};
`;

const Patient = ({professional, children}) => {
  return (
    <Line professional = {} >
      {children}
      <ButtonDelete/>
    </Line>
  );
};

export default Patient; */